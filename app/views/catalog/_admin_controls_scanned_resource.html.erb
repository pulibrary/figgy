<% if ChangeSet.for(resource).is_a?(RecordingChangeSet) %>
  <%= render "admin_controls_recording", resource: resource %>
<% else %>
  <%= render "admin_controls_default", resource: resource %>
<% end %>
