<% if DynamicChangeSet.new(resource).is_a?(MediaReserveChangeSet) %>
  <%= render "admin_controls_media_reserve", resource: resource %>
<% else %>
  <%= render "admin_controls_default", resource: resource %>
<% end %>