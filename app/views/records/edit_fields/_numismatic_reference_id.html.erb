<%= f.input :numismatic_reference_id,
            label: 'Numismatic Reference',
            as: :select,
            collection: @numismatic_references,
            label_method: :short_title,
            value_method: :id,
            input_html: { multiple: f.object.multiple?(key) },
            include_blank: true,
            selected: f.object.numismatic_reference_id,
            required: f.object.required?(key) %>
