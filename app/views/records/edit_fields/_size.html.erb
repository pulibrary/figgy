<% if f.object.multiple? key %>
  <%= f.input key, as: :multi_value, label: 'Size (mm)', input_html: { class: 'form-control' }, required: f.object.required?(key) %>
<% else %>
  <%= f.input key, label: 'Size (mm)', required: f.object.required?(key) %>
<% end %>
