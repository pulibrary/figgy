  <%= f.input key,
              as: :select,
              collection: ControlledVocabulary.for(:language).all,
              label_method: :label,
              value_method: :value,
              hint: "One caption must exist in the original language of the audio",
              include_blank: true,
              input_html: { multiple: true, class: 'form-control' },
              required: f.object.required?(key) %>
