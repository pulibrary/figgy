PreservationAudit#complete

Preservation Audit batch is complete, but some jobs failed. Check the sidekiq UI for failures. They will retry and another email will be delivered if they all succeed, or if any of them is moved to the dead queue.

<%= link_to("Show this preservation audit", @audit) %>
